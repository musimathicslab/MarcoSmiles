using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NextNotesContentScript : MonoBehaviour
{
    [SerializeField]
    public List<TMPro.TextMeshProUGUI> nextNotesText;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
